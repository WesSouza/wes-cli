import { resolve } from 'path';

export const CONFIG_FILENAME = 'wes-config.json';

export const LOCAL_OVERRIDES_LOCATION = '.wes-defaults/local';

export const MODULES_PATH = resolve(__dirname, '../modules');
